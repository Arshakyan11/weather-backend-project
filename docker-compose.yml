services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
    restart: on-failure:3
  redis:
    image: redis
    ports:
      - "6379:6379"
    restart: on-failure:3
